# cicd-distribution

Public binary distribution and update metadata for cicd desktop releases.

This repository is generated by the private source repo release workflow.

- `stable/latest.json` is the machine-readable update manifest.
- `stable/linux/` contains the Linux IFW online repository.
- `stable/windows/` contains the Windows IFW online repository.
- `downloads/` contains public installer binaries.
